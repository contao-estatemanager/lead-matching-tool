services:
    _defaults:
        autowire: true
        autoconfigure: true

    ContaoEstateManager\LeadMatchingTool\:
        resource: ../../
        exclude: ../../{ContaoManager,Model,Resources}
